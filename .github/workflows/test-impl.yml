name: ⚙️ test
on:
  workflow_call:
    inputs:
      pre_commit:
        description: "Run pre-commit."
        required: true
        type: string
      unit_tests:
        description: "Run unit tests"
        required: true
        type: string
      invocation_tests:
        description: "Run invocation tests."
        required: true
        type: string
      integration_tests:
        description: "Run integration-like workflow submission tests."
        required: true
        type: string
      unit_test_args:
        description: "CLI args to pass verbatim to pytest (unit tests)."
        required: false
        type: string
        default: ""
      integration_test_args:
        description: "CLI args to pass verbatim to pytest (integration tests)."
        required: false
        type: string
        default: ""
      ref:
        required: true
        type: string
        description: Git commit reference.
      head-ref:
        required: false
        type: string
        description: Git HEAD reference (branch name).
      full-repo-name:
        required: true
        type: string
        description: Full repository name.
      executable_name:
        required: true
        type: string
        description: Template parameter.
      pytest_args:
        required: true
        type: string
        description: Template parameter.
      CLI_path:
        required: true
        type: string
        description: Template parameter.
      CLI_path_win:
        required: true
        type: string
        description: Template parameter.
      CLI_module:
        required: true
        type: string
        description: Template parameter.
      app_package:
        required: true
        type: string
        description: Template parameter.
    secrets:
      pre-commit-token:
        required: true
        description: >
          PAT of user who has permission to bypass branch protection,
          or standard GITHUB_TOKEN if running on an external fork (won't
          be able to commit fixes)
  
# e.g. don't run simultaneously on the same branch (since we may commit to that branch)
concurrency: ci-${{ inputs.head-ref && format('refs/heads/{0}', inputs.head-ref) || inputs.ref }}

env:
  PYTHON_VERSION_PRE_COMMIT: "3.12"
  POETRY_VERSION: "1.4"

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    if: inputs.pre_commit == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.pre-commit-token }}
          # checkout PR source branch (head_ref) if event is pull_request:
          ref: ${{ inputs.head-ref || inputs.ref }}
          repository: ${{ inputs.full-repo-name }}
      - uses: hpcflow/github-support/configure-git@0.1
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION_PRE_COMMIT }}

      - name: pre-commit
        # avoid exit code 1 (which halts GH actions) from pre-commit run command by
        # running twice:
        run: |
          pip install pre-commit
          pre-commit install
          export SKIP=no-commit-to-branch
          pre-commit run --all-files || pre-commit run --all-files

      - name: pre-commit push changes
        run: |
          if git diff --quiet; then
            echo "No pre-commit changes"
          else
            git commit -am "pre-commit fixes [skip ci]"
            git push
          fi

  test-units:
    needs: pre-commit
    if: inputs.unit_tests == 'true'
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
        os:
          - ubuntu-latest
          - macos-13
          - windows-latest

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: hpcflow/github-support/setup-poetry@0.1
        with:
          version: ${{ env.POETRY_VERSION }}
      - uses: hpcflow/github-support/init-cache@0.1
        with:
          name: test
          version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          poetry install --without dev,pyinstaller

      - name: Run tests
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          poetry run python -m pytest ${{ inputs.pytest_args }} ${{ inputs.unit_test_args }}

  test-units-CentOS:
    needs: pre-commit
    runs-on: ubuntu-latest
    if: inputs.unit_tests == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - uses: hpcflow/github-support/init-cache@0.1
        with:
          name: test
          version: CentOS

      - name: Run unit tests within Docker
        uses: addnab/docker-run-action@v3
        with:
          image: ghcr.io/hpcflow/centos7-poetry:latest
          options: -v ${{ github.workspace }}:/home --env GH_TOKEN=${{ secrets.GITHUB_TOKEN }}
          run: |
            cd /home
            poetry config virtualenvs.in-project true
            poetry config installer.modern-installation false
            poetry install --without dev,pyinstaller
            poetry run python -m pytest ${{ inputs.unit_test_args }}

  test-integration:
    needs: pre-commit
    if: inputs.integration_tests == 'true'
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
        os:
          - ubuntu-latest
          - macos-13
          - windows-latest

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: hpcflow/github-support/setup-poetry@0.1
        with:
          version: ${{ env.POETRY_VERSION }}
      - uses: hpcflow/github-support/init-cache@0.1
        with:
          name: test-integration
          version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          poetry install --without dev,pyinstaller

      - name: Run tests
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          poetry run ${{ inputs.executable_name }} test ${{ inputs.pytest_args }} --integration ${{ inputs.integration_test_args }}

  test-integration-CentOS:
    needs: pre-commit
    runs-on: ubuntu-latest
    if: inputs.integration_tests == 'true'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - uses: hpcflow/github-support/init-cache@0.1
        with:
          name: test-integration
          version: CentOS

      - name: Run integration tests within Docker
        uses: addnab/docker-run-action@v3
        with:
          image: ghcr.io/hpcflow/centos7-poetry:latest
          options: -v ${{ github.workspace }}:/home --env GH_TOKEN=${{ secrets.GITHUB_TOKEN }}
          run: |
            cd /home
            poetry config virtualenvs.in-project true
            poetry config installer.modern-installation false
            poetry install --without dev,pyinstaller
            poetry run hpcflow test --integration ${{ inputs.integration_test_args }}

  test-invocation-ubuntu:
    if: inputs.invocation_tests == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: hpcflow/github-support/setup-poetry@0.1
        id: python
        with:
          version: ${{ env.POETRY_VERSION }}
      - uses: hpcflow/github-support/init-cache@0.1
        with:
          name: test-invocation
          version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          poetry install --without dev,pyinstaller
          poetry run pip install ipython

      - name: Prepare test data
        id: prep-test-data
        run: |
          echo "exe-path=${PY//python/$EXE_NAME}" >> $GITHUB_OUTPUT
        env:
          EXE_NAME: ${{ inputs.executable_name }}
          PY: ${{ steps.python.outputs.python-path }}

      - name: Test invocation command with `python ./${{ inputs.CLI_path }}`
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./${{ inputs.CLI_path }} internal get-invoc-cmd
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with `python -m ${{ inputs.CLI_module }}`
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python -m ${{ inputs.CLI_module }} internal get-invoc-cmd
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with `${{ inputs.executable_name }}` entry point script
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: ${{ inputs.executable_name }} internal get-invoc-cmd
          expected: "('${{ steps.python.outputs.python-path }}', '${{ steps.prep-test-data.outputs.exe-path }}')"

      - name: Test invocation command within a python script
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./.github/workflows/get_invoc_cmd.py ${{ inputs.app_package }}
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with interactive python
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./.github/workflows/get_invoc_cmd_interactive.py python ${{ inputs.app_package }}
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with interactive ipython
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./.github/workflows/get_invoc_cmd_interactive.py ipython ${{ inputs.app_package }}
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test direct workflow submission within a python script
        run: |
          source $(poetry env info --path)/bin/activate
          python ./.github/workflows/test_direct_sub_python_script.py ${{ inputs.app_package }}

      - name: Test direct workflow submission within a Jupyter notebook (via papermill)
        run: |
          source $(poetry env info --path)/bin/activate
          python -m ipykernel install --user --name python3
          papermill ./.github/workflows/test_direct_sub_jupyter_notebook.ipynb ./.github/workflows/test_direct_sub_jupyter_notebook_${{ matrix.python-version }}_${{ runner.os }}.ipynb -p app_import_str ${{ inputs.app_package }}

  test-invocation-macos:
    if: inputs.invocation_tests == 'true'
    runs-on: macos-13
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: hpcflow/github-support/setup-poetry@0.1
        id: python
        with:
          version: ${{ env.POETRY_VERSION }}
      - uses: hpcflow/github-support/init-cache@0.1
        with:
          name: test-invocation
          version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          poetry install --without dev,pyinstaller
          poetry run pip install ipython

      - name: Prepare test data
        id: prep-test-data
        run: |
          echo "exe-path=${PY//python/$EXE_NAME}" >> $GITHUB_OUTPUT
        env:
          EXE_NAME: ${{ inputs.executable_name }}
          PY: ${{ steps.python.outputs.python-path }}

      - name: Test invocation command with `python ./${{ inputs.CLI_path }}`
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./${{ inputs.CLI_path }} internal get-invoc-cmd
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with `python -m ${{ inputs.CLI_module }}`
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python -m ${{ inputs.CLI_module }} internal get-invoc-cmd
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with `${{ inputs.executable_name }}` entry point script
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: ${{ inputs.executable_name }} internal get-invoc-cmd
          expected: "('${{ steps.python.outputs.python-path }}', '${{ steps.prep-test-data.outputs.exe-path }}')"

      - name: Test invocation command within a python script
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./.github/workflows/get_invoc_cmd.py ${{ inputs.app_package }}
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with interactive python
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./.github/workflows/get_invoc_cmd_interactive.py python ${{ inputs.app_package }}
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test invocation command with interactive ipython
        uses: hpcflow/github-support/compare@0.1
        with:
          init: source $(poetry env info --path)/bin/activate
          command: python ./.github/workflows/get_invoc_cmd_interactive.py ipython ${{ inputs.app_package }}
          expected: "('${{ steps.python.outputs.python-path }}', '${{ github.workspace }}/${{ inputs.CLI_path }}')"

      - name: Test direct workflow submission within a python script
        run: |
          source $(poetry env info --path)/bin/activate
          python ./.github/workflows/test_direct_sub_python_script.py ${{ inputs.app_package }}

      - name: Test direct workflow submission within a Jupyter notebook (via papermill)
        run: |
          source $(poetry env info --path)/bin/activate
          python -m ipykernel install --user --name python3
          papermill ./.github/workflows/test_direct_sub_jupyter_notebook.ipynb ./.github/workflows/test_direct_sub_jupyter_notebook_${{ matrix.python-version }}_${{ runner.os }}.ipynb -p app_import_str ${{ inputs.app_package }}

  test-invocation-windows:
    if: inputs.invocation_tests == 'true'
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: hpcflow/github-support/setup-poetry@0.1
        id: poetry
        with:
          version: ${{ env.POETRY_VERSION }}
      - uses: hpcflow/github-support/init-cache@0.1
        with:
          name: test-invocation
          version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          poetry install --without dev,pyinstaller

      - name: Prepare test data
        id: prep-test-data
        run: |
          ("python-path=" + $Env:PY.Replace('\', '\\')) | Out-File $Env:GITHUB_OUTPUT -Append
          ("cli-path=$Env:GITHUB_WORKSPACE\$Env:CLI_PATH".Replace('\', '\\')) | Out-File $Env:GITHUB_OUTPUT -Append
          ("exe-path=$Env:PY".Replace('python.exe', $Env:EXE_NAME).Replace('\', '\\')) | Out-File $Env:GITHUB_OUTPUT -Append
        env:
          CLI_PATH: ${{ inputs.CLI_path_win }}
          EXE_NAME: ${{ inputs.executable_name }}
          PY: ${{ steps.poetry.outputs.python-path }}

      - name: Test invocation command with `python ./${{ inputs.CLI_path }}`
        uses: hpcflow/github-support/compare@0.1
        with:
          init: ."$(poetry env info --path)\Scripts\activate.ps1"
          command: python ./${{ inputs.CLI_path }} internal get-invoc-cmd
          expected: "('${{ steps.prep-test-data.outputs.python-path }}', '${{ steps.prep-test-data.outputs.cli-path }}')"

      - name: Test invocation command with `python -m ${{ inputs.CLI_module }}`
        uses: hpcflow/github-support/compare@0.1
        with:
          init: ."$(poetry env info --path)\Scripts\activate.ps1"
          command: python -m ${{ inputs.CLI_module }} internal get-invoc-cmd
          expected: "('${{ steps.prep-test-data.outputs.python-path }}', '${{ steps.prep-test-data.outputs.cli-path }}')"

      - name: Test invocation command with `${{ inputs.executable_name }}` entry point script
        uses: hpcflow/github-support/compare@0.1
        with:
          init: ."$(poetry env info --path)\Scripts\activate.ps1"
          command: ${{ inputs.executable_name }} internal get-invoc-cmd
          expected: "('${{ steps.prep-test-data.outputs.python-path }}', '${{ steps.prep-test-data.outputs.exe-path }}')"

      - name: Test invocation command within a python script
        uses: hpcflow/github-support/compare@0.1
        with:
          init: ."$(poetry env info --path)\Scripts\activate.ps1"
          command: python .\.github\workflows\get_invoc_cmd.py ${{ inputs.app_package }}
          expected: "('${{ steps.prep-test-data.outputs.python-path }}', '${{ steps.prep-test-data.outputs.cli-path }}')"

      - name: Test direct workflow submission within a python script
        run: |
          ."$(poetry env info --path)\Scripts\activate.ps1"
          python .\.github\workflows\test_direct_sub_python_script.py ${{ inputs.app_package }}

      - name: Test direct workflow submission within a Jupyter notebook (via papermill)
        run: |
          ."$(poetry env info --path)\Scripts\activate.ps1"
          python -m ipykernel install --user --name python3
          papermill .\.github\workflows\test_direct_sub_jupyter_notebook.ipynb .\.github\workflows\test_direct_sub_jupyter_notebook_${{ matrix.python-version }}_${{ runner.os }}.ipynb -p app_import_str ${{ inputs.app_package }}

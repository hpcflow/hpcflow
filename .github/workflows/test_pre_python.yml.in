name: Test pre-python
on:
  workflow_dispatch: # manual invocation
jobs:
  test:
    uses: ./.github/workflows/test-pre-python-impl.yml
    with:
      ref: ${{ github.ref }}
      head-ref: ${{ github.head_ref }}
      pytest-args: {{ pytest_args }}
